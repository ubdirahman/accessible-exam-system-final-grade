PORT=8000
MODEL_NAME=sentence-transformers/all-MiniLM-L6-v2
HF_TOKEN=hf_xxx          # set your HF token to remove warnings and speed downloads
HF_HOME=./hf-cache       # cache folder; will be created if missing
PRELOAD_MODEL=true       # set false to lazy-load on first request
